# Suggested Commands

- Run tests from repo root: `pytest`.
- Manual ingest smoke: `kgdb ingest --input desk/fixtures/substrate_v1.json --output /tmp/kgdb-substrate.kg.json`.
- If console script is unavailable in a fresh checkout, install editable package first: `python -m pip install -e .`.